#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, x;
    cin >> n;
    while (n--) {
        cin >> hex >> x;
        cout << char(x);
    }
    cout << endl;
    return 0;
}