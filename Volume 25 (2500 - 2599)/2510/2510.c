#include <stdio.h>
#include <string.h>
 
int main() {
    int n, i;
    char s[256];
    scanf("%d", &n);
    while(n--) {
        scanf("%s", s);
        printf("Y\n");
    }
    return 0;
}