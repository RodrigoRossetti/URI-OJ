#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n, i = 1;
	while (cin >> n && n) cout << "Teste " << i++ << endl << (int)pow(2, n)-1 << endl << endl;
	return 0;
}